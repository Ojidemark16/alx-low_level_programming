#include <stdio.h>

int main(void)
/**
* main - Entry point
* Description: a program the print the alphabet in a lowercase
* Return: Always 0 (success)
*/
{char x;
for (x = 'a'; x <= 'z'; x++)
putchar(x);
return (0);
}
